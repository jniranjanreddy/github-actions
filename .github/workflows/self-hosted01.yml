name: kubectl test

on:
  workflow_dispatch:
  push:
    branches:
      - main
jobs:
  test:
    runs-on: [self-hosted, ubuntu24]
    steps:
      - name: Setup kubeconfig
        run: |
           kubectl --kubeconfig=/tmp/kubeconfig get pods -A
