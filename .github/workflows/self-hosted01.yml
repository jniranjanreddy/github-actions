name: Test Runner

on:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: [self-hosted, Linux]

    steps:
      - name: Test message
        run: echo "✅ Self-hosted runner is working!"

      - name: System info
        run: |
          echo "User: $(whoami)"
          echo "Machine: $(hostname)"
