name: Test Runner

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: [self-hosted, ubuntu2]

    steps:
      - name: Test message
        run: echo "✅ Self-hosted runner is working!"

      - name: System info
        run: |
          echo "User: $(whoami)"
          echo "Machine: $(hostname)"
